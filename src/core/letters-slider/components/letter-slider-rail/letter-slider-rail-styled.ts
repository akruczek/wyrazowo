import styled from 'styled-components/native'
import { COLOR } from '../../../colors/colors.constants'

export const LetterSliderRail = styled.View`
  height: 10px;
  border-radius: 5px;
  width: 100%;
  background-color: ${COLOR.DODGER_BLUE};
`
