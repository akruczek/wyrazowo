/**
 * Returns true if given argument is greater than the second, false otherwise
 * @example
 * ```typescript
 * gt(3, 3) //=> false
 * gt(3, 4) //=> true
 * ```
 */

export const gt = (
  number2: number,
  number1: number,
): boolean => number1 > number2
